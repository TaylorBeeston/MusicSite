<%= link_to("Back", songs_path)%>
<h1><%= @song.name %></h1>
<p><%= @song.bio %></p>
<%= image_tag(@song.picture.url, height: 50, width: 50) %>
<%= link_to("Edit", edit_song_path(@song), class: "btn btn-secondary") %>
<%= link_to("Delete", @song, method: "delete", class: "btn btn-danger") %>
