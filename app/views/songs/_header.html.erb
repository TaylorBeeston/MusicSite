<nav class="navbar fixed-top navbar-dark bg-dark">
  <div class="d-none d-md-inline col-2">
    <span class="navbar-brand">All Songs</span>
  </div>
  <div class="col-2">
    <span class="text-white">Song</span>
  </div>
  <div class="col offset-1">
    <span class="text-white">Song Description</span>
  </div>
  <% if logged_in? %>
    <%= link_to("New", new_song_path, class: "nav-link", data: { confirm: stopSongText }) %>
    <%= link_to("Menu", menu_path, class: "nav-link", data: { confirm: stopSongText }) %>
    <%= link_to("Logout", logout_path, class: "nav-link", method: :delete, 
                                       data: { confirm: stopSongText }) %>
  <% else %>
    <%= link_to("Login", login_path, class: "nav-link", data: { confirm: stopSongText }) %>
  <% end %>
</nav>
